import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.sepher.app',
  appName: 'RegistrAPP',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
